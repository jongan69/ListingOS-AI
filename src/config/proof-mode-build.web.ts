// The hosted web app keeps the real seller flow available. Proof scenarios are
// mounted alongside it and become non-mutating based on their fixture IDs.
export const proofModeBuildEnabled = false;
